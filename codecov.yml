coverage:
  status:
    project:
      default:
        target: 92%
        threshold: 1%
    patch:
      default:
        target: 92%

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: true
